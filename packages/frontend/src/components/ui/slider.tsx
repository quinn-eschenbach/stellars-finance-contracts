/**
 * react95 Slider, re-exported under the local ui path. Note the react95
 * onChange signature: `(value: number) => void`, not a DOM change event.
 */
export { Slider, type SliderProps } from "react95";
