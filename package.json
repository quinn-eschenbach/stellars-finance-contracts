{
  "name": "stellars-finance",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=18"
  }
}
